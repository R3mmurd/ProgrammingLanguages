(* Programming Languages *)
(* Lecture 01: Some errors *)

(* This program has several errors in it so we can try to debug them *)

val x = 24
y = x + 1
val z = if y then 24 else x < 4
val q = if y > 0 then 0
val a = -5
val w = 0
val fun = 24
val v = x / w
val fourteen = 7 - 7